# Laravel and Boostrap

This is a Laravel code for Leaderboard with Bootstrap.

```bash
composer install
php artisan migrate
php artisan db:seed 
php artisan serve
```

## via browser

```python
http://localhost:8000/
